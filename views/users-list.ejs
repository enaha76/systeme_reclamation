<h1>All Users</h1>

<% if (users.length > 0) { %>
  <ul>
    <% users.forEach(user => { %>
      <li>
        <p>Name: <%= user.name %></p>
        <p>Email: <%= user.email %></p>
      </li>
    <% }) %>
  </ul>
<% } else { %>
  <p>No users found.</p>
<% } %>
